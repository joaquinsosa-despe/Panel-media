{
  "name": "despegar-media",
  "version": "1.0.0",
  "description": "Despegar Media Sales — Google Apps Script (Slides + Web App Dashboard)",
  "private": true,
  "scripts": {
    "pull": "clasp pull",
    "push": "clasp push --force",
    "push:watch": "clasp push --watch --force",
    "open": "clasp open",
    "logs": "clasp logs",
    "deploy": "clasp deploy"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
